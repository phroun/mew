// Command mew-sdl is the graphical (SDL) mew application: a KittyTK SDL-mode
// host that opens a maximized window holding a root-level mew editor, and
// serves the KittyTK protocol so other apps can connect and embed their own
// (sub-)mew editor trinkets.
//
// Scaffold only for now — the host bootstrap lands once the KittyTK fork's
// SDL host is wired in. The command-line mew (cmd/mew) is the TUI twin.
package main

import "fmt"

func main() {
	fmt.Println("mew-sdl: not yet implemented (graphical KittyTK host — scaffold)")
}
